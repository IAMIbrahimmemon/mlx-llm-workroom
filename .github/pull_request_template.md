## What this changes

<!-- One or two sentences. -->

## Checks

- [ ] `uv run pytest -q` passes
- [ ] `cd macapp && swift build` is warning-free
- [ ] Touched an onboarding screen? Re-rendered with `--snapshot` and looked at it
- [ ] Any shell execution still goes through `Guard.check_shell`
- [ ] Nothing new leaves the machine
